import React from 'react';
import './HeroSection.css';

const HeroSection = () => {
  return (
    <section className="hero-section">
      <div className="hero-overlay">
        <h1 className="hero-title">Nestling</h1>
        <p className="hero-subtitle">Empowering Growth, One Story at a Time!</p>
      </div>
    </section>
  );
};

export default HeroSection;

